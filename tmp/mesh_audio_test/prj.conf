# Bisection step 4 (final): shell + SD card + VS1053 + BT Mesh. This
# combines everything already proven working in isolation
# (tmp/shell_test, tmp/sd_test, tmp/audio_test) with the mesh chat
# model, unchanged from the main MeshyQ app.

CONFIG_NCS_SAMPLES_DEFAULTS=y
CONFIG_LOG_MODE_DEFERRED=y

CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_NVS=y
CONFIG_SETTINGS=y
CONFIG_NVS_LOOKUP_CACHE=y
CONFIG_SETTINGS_NVS_NAME_CACHE=y
CONFIG_HWINFO=y
CONFIG_GPIO=y
CONFIG_SPI=y

# SD card (Music Maker FeatherWing) storage
CONFIG_DISK_ACCESS=y
CONFIG_FILE_SYSTEM=y
CONFIG_FAT_FILESYSTEM_ELM=y

# Bluetooth configuration
CONFIG_BT=y
CONFIG_BT_DEVICE_NAME="MeshyQ"
CONFIG_BT_L2CAP_TX_BUF_COUNT=16
CONFIG_BT_OBSERVER=y
CONFIG_BT_PERIPHERAL=y
CONFIG_BT_SETTINGS=y

# Disable unused Bluetooth features
CONFIG_BT_CTLR_LE_ENC=n
CONFIG_BT_PHY_UPDATE=n
CONFIG_BT_CTLR_CHAN_SEL_2=n
CONFIG_BT_CTLR_MIN_USED_CHAN=n
CONFIG_BT_CTLR_PRIVACY=n

# Bluetooth Mesh configuration
CONFIG_BT_MESH=y
CONFIG_BT_MESH_RELAY=y
CONFIG_BT_MESH_FRIEND=y

# Mesh buffer and segment configuration (fixes "Unable to allocate loopback")
CONFIG_BT_MESH_LOOPBACK_BUFS=8
CONFIG_BT_MESH_RX_SEG_MAX=16
CONFIG_BT_MESH_TX_SEG_MAX=16
CONFIG_BT_MESH_TX_SEG_MSG_COUNT=4

CONFIG_BT_MESH_PB_GATT=y
CONFIG_BT_MESH_GATT_PROXY=y
CONFIG_BT_MESH_CFG_CLI=y

# Enable Bluetooth Mesh models debug logs
CONFIG_BT_MESH_LOG_LEVEL_DBG=y

# Enable Shell module, USB CDC-ACM backend comes from the board's own
# uf2 defconfig (same as tmp/shell_test and every step since).
CONFIG_SHELL=y
CONFIG_SHELL_BACKEND_SERIAL=y
CONFIG_FLASH_SHELL=n

CONFIG_LOG_BACKEND_RTT=n

# Same sizing already proven necessary/sufficient in tmp/sd_test and
# tmp/audio_test for FAT + SD-over-SPI, carried over unchanged. Bumped
# a bit further here since this path also brings up Bluetooth/mesh in
# the same boot sequence.
CONFIG_MAIN_STACK_SIZE=8192
CONFIG_HEAP_MEM_POOL_SIZE=4096
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

# So a stack overflow shows up as a clear fault message instead of a
# silent crash/reset, if it happens again.
CONFIG_HW_STACK_PROTECTION=y
