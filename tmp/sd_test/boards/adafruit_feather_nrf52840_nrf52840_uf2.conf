# Same fix as blinky/shell_test/MeshyQ: without this, Zephyr ignores the
# board's devicetree partition layout and links the app at address 0,
# which overlaps the Adafruit bootloader and bricks the board.
CONFIG_USE_DT_CODE_PARTITION=y
