# Same fix as blinky/MeshyQ: without this, Zephyr ignores the board's
# devicetree partition layout and links the app at address 0, which
# overlaps the Adafruit bootloader and bricks the board when flashed.
CONFIG_USE_DT_CODE_PARTITION=y
