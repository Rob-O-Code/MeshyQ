# Bisection step 1: shell/serial only. No Bluetooth, no SPI, no SD, no
# audio. Everything else is left at board defaults (same as blinky) so
# the only thing under test is the shell over USB CDC-ACM.
CONFIG_SHELL=y
CONFIG_SHELL_BACKEND_SERIAL=y
